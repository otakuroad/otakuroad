{
  "name": "otakuroad",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Akihabara subculture store map",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "npm run build:data",
    "dev": "astro dev",
    "prebuild": "npm run validate && npm run build:data",
    "build": "astro build",
    "preview": "astro preview",
    "precheck": "npm run build:data",
    "check": "astro check",
    "validate": "tsx scripts/validate.ts",
    "audit": "tsx scripts/audit.ts",
    "build:data": "tsx scripts/build-data.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@astrojs/svelte": "^9.0.1",
    "astro": "^7.3.1",
    "fuse.js": "^7.5.0",
    "maplibre-gl": "^6.7.0",
    "svelte": "^5.57.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^22.0.0",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "vitest": "^5.0.0"
  }
}
